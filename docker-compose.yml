name: ${APP_NAME:-qf-pcr}

services:

  app:
    build:
      context: .
      target: dev
      platforms:
        - linux/amd64
        - linux/arm64

    command: npm run start

    environment:
      - DEBUG=true
      - CI=false
      - CHOKIDAR_USEPOLLING=true
      - WATCHPACK_POLLING=true
      - NODE_ENV=development
      - GENERATE_SOURCEMAP=false

    volumes:
      - ./src:/app/src
      - ./public:/app/public

    ports:
      - "3000:3000"
