apiVersion: v1
kind: Service
metadata:
  name: {{ include "qf-pcr.fullname" . }}
  labels:
    {{- include "qf-pcr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "qf-pcr.selectorLabels" . | nindent 4 }}
